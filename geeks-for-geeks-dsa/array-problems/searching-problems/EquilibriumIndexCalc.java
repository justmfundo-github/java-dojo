/*
 * Given an array arr[] of size n, return an equilibrium index (if any) or -1 if no equilibrium index exists. The equilibrium index of an array is an index such that the sum of elements at lower indexes equals the sum of elements at higher indexes.
 * Return equilibrium point in 1-based indexing. Return -1 if no such point exists.
 * 
 * In short, value returned when adding all values to the left of a midpoint of an array should equal
 * the sum of all the values to the right of the midpoint
 */


public class EquilibriumIndexCalc {
  
}
